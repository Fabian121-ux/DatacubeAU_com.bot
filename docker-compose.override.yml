services:
  postgres:
    ports:
      - "127.0.0.1:${POSTGRES_PORT:-5432}:5432"

  api:
    ports:
      - "127.0.0.1:${API_PORT:-8080}:8080"

  waha:
    ports:
      - "127.0.0.1:${WAHA_HTTP_PORT:-3000}:3000"
